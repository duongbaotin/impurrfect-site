# impurrfect-site — AGENTS.md

## Stack

- **Astro 6** + **Tailwind CSS v4** (via `@tailwindcss/vite`). No React, no other framework.
- Node >= 22.12.0. ESM (`"type": "module"`).
- TypeScript via `astro/tsconfigs/strict`.

## Commands

| Command | Action |
|---------|--------|
| `npm run dev` | Dev server at `localhost:4321` |
| `npm run build` | Build to `./dist/` |
| `npm run preview` | Preview production build |
| `npm run check` | Type-check all `.astro` files via `@astrojs/check` |
| `npm run astro` | CLI wrapper (`astro add`, etc.) |

No tests, no linter, no formatter config in this repo.

## Routes

- `/` — landing page
- `/menu` — cocktail menu listing
- `/about` — brand story
- `/contact` — contact info and inquiry note
- `*` — 404 page (branded)

## Architecture

- `src/layouts/MainLayout.astro` — shared HTML shell with dark theme (`bg-[#0F0F12]`), `<Nav />`, `<Footer />`, SEO meta tags, `<ClientRouter />` (Astro 6 — not `ViewTransitions`), skip-link utility.
- Expects optional `title`, `description`, and `allergyNotice` (boolean) props. `allergyNotice` passed to `<Footer />`.
- Font utility classes (`.font-display`, `.font-body`, `.font-accent`) defined in `MainLayout`'s `<style is:global>`. **Do not** use Tailwind's `fontFamily` theme config.
- Only **Playfair Display** has a Google Fonts link in `MainLayout`. Satoshi and Freight Big Pro are declared as font-family with system fallbacks but are not loaded by the repo — they resolve to fallback fonts.
- Brand colors (`#0F0F12`, `#F6F3EF`, `#875A2A`, `#33363A`) via Tailwind arbitrary values (`bg-[#0F0F12]`). No CSS custom properties.
- `src/styles/global.css` = single line `@import "tailwindcss"`. Imported only by `MainLayout`.
- Sitemap auto-generated by `@astrojs/sitemap`. Config has `site: 'https://impurrfect-bar.meowracle.space'`.
- Vercel deployment via `vercel.json` with `cleanUrls: true`.
- `src/components/HoursLocation.astro` — pre-built venue info block. Import instead of duplicating markup.

## Content Collections

Menu data lives in Content Collections so items are type-safe and easy to add or reorder.

| File | Purpose |
|---|---|
| `src/content.config.ts` | Collection schema (zod via `astro/zod`), `glob` loader matches `src/content/menu/*.yaml` |
| `src/content/menu/*.yaml` | One file per menu item |
| `src/data/categories.ts` | Category metadata (name, description, sort order) |
| `src/data/menu.ts` | Shared TypeScript types (`MenuItemData`, `MenuCategoryData`) |

### YAML fields

```yaml
name: string
price: string          # e.g. "VND 180k"
description: string
category: string       # must match a name in categories.ts
order: number          # sort position within category
tags?: string[]        # e.g. ["Smoky", "Bitter"]
featured?: boolean     # shows ★ Bartender's Pick badge
image?: string         # path to thumbnail in public/, e.g. "/cocktail-placeholder.svg"
subcategory?: string   # optional grouping within a category, e.g. "Whiskey-based"
```

To add a new menu item: create a new `.yaml` file, commit, and Vercel auto-deploys.

## Style conventions

- All colors via Tailwind arbitrary values (`bg-[#0F0F12]`), never theme variables.
- Font classes via `.font-display`, `.font-body`, `.font-accent` utilities.
- Price formatting: Vietnamese Dong with "k" shorthand, e.g. `VND 180k`.
- Images in `public/` referenced as absolute paths (`/hero.svg`).

## Infrastructure

- **Live domain:** `impurrfect-bar.meowracle.space`
- **DNS:** Cloudflare (proxied, orange cloud). Root `meowracle.space` has an A record forwarding to Cloudflare's edge.
- **Redirect:** Cloudflare Page Rule — `meowracle.space/impurrfect-bar*` → `https://impurrfect-bar.meowracle.space/$1` (301). This is independent of this repo; do not remove.
- **Root domain** (`meowracle.space`) is a separate future project, not this repo.

## References

- **`CONTENT-GUIDE.md`** — end-user guide for editing page text, menu items, prices, categories, and venue info. Keep in sync if the codebase structure changes.

## Generated files (do not edit)

- `.astro/` — Astro-generated type stubs, settings
- `dist/` — build output
- `.vercel/` — Vercel output (also in `.gitignore`)
